<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <style>
      /* BASIC */
      html, body {
          margin: 0;
          padding: 0;
          height: 100%;
      }

      body {
          font-family: Arial, sans-serif;
          display: flex;
          justify-content: center;
          align-items: center;
          background-image: linear-gradient(-45deg, #35C3F3 0%, #8b9fe8 20%, #e681d8 39%, #ffa9a4 76%, #FED2CE 100%);
          color: #fff;
      }

      /* STRUCTURE */
      .wrapper {
          width: 100%;
          max-width: 400px;
          text-align: center;
          padding: 20px;
      }

      #formContent {
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.2);
          padding: 30px;
          -webkit-box-shadow: 0 30px 60px 0 rgba(0, 0, 0, 0.3);
          box-shadow: 0 30px 60px 0 rgba(0, 0, 0, 0.3);
      }

      /* TABS */
      h2 {
          font-size: 24px;
          margin-bottom: 20px;
      }

      /* FORM */
      input[type="text"], input[type="password"] {
          background-color: rgba(255, 255, 255, 0.2);
          border: none;
          color: #fff;
          padding: 15px;
          text-align: center;
          font-size: 16px;
          margin: 10px 0;
          width: 100%;
          border-radius: 5px;
          outline: none;
      }

      input[type="submit"] {
          background-color: rgba(255, 255, 255, 0.4);;
          border: none;
          color: #fff;
          padding: 15px;
          text-align: center;
          font-size: 16px;
          margin-top: 20px;
          width: 100%;
          border-radius: 5px;
          cursor: pointer;
          transition: background-color 0.3s ease;
      }

      input[type="submit"]:hover {
          background-color: rgba(255, 255, 255, 0.2);;
      }

      ::placeholder {
          color: #fff;
          opacity: 1;
      }

      /* LINK */
      .underlineHover {
          color: #fff;
          text-decoration: none;
          transition: color 0.3s ease;
      }

      .underlineHover:hover {
          color: gainsboro;
;
      }

      /* LOGO */
      #icon {
          width: 110px;
          margin-bottom: 20px;
      }

      /* ALERTS */
      .alert {
        padding: 20px;
        border: 0;
        border-radius: 8px;
        margin-top: 20px;
        margin-bottom: 20px;
      }

      .btn-close {
        padding-right: 20px;
        background-color: transparent;
        border: 0;
        color: #fff;
        font-size: 1.5rem;
        cursor: pointer;
      }

      .alert-danger {
        background-color: rgba(255, 50, 50, 0.75);
        color: #fff;
      }

      /* ANIMATIONS */
      #formContent {  
        opacity: 0; 
        animation: fadeIn 0.8s ease-in forwards;
        animation-delay: 0.3s;
      }

      h2, #icon {
        opacity: 0;
        animation: fadeIn 0.8s ease-in forwards;
        animation-delay: 0.2s; 
      }

      input[type="text"], input[type="password"], input[type="submit"] {
        opacity: 0;
        animation: fadeIn 0.8s ease-in forwards;
        animation-delay: 0.5s;
      }

      @keyframes fadeIn {
        from {opacity: 0; }
        to   {opacity: 1; }
      }
  </style>
  <title>Sign Up</title>
</head>
<body>

<div class="wrapper">
  <div id="formContent">
    <div class="container-fluid align-content-center"> 
      <h2>Right to Repair</h2> 
      <img src="https://aftrr.org/wp-content/uploads/2017/02/right-to-repair-hand-wrench-30.png" id="icon" alt="Logo" />
    </div>
    <!-- Login Form -->
    <form method="post" action="/signup/register">
        <input type="text" id="login" name="username" placeholder="Username" required>
        <input type="password" id="password" name="password" placeholder="Password" required>
        <input type="submit" value="Create Account">
    </form>
    <% if(msg && msg==="error") { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
      <p class="mb-0">That username is already taken! Please enter another.</p>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">&times</button>
    </div>
    <% } %>
    <!-- Remind Passowrd -->
    <div id="formFooter">
      Already have an account? <a class="underlineHover" href="/">Login.</a>
    </div>
  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

</body>
</html>